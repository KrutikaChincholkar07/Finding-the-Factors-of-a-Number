# Finding-the-Factors-of-a-Number
This program finds all the factors (divisors) of a number entered by the user. A factor of a number is any integer that divides the number exactly without leaving a remainder. The program checks numbers from 1 to the given number and prints all values that divide the number evenly.
